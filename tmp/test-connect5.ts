console.log('START');
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/streamableHttp.js';
console.log('After imports');
const server = new McpServer({ name: 'test', version: '0.1.0' }, { capabilities: { tools: {} } });
console.log('Server created');
const transport = new StreamableHTTPServerTransport({ sessionIdGenerator: () => 'test-id' });
console.log('Transport created');
server.connect(transport).then(() => console.log('Connected')).catch(e => console.error('Error:', e));
console.log('After connect call');
